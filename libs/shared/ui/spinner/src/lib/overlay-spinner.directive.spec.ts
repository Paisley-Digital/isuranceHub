import { OverlaySpinnerDirective } from './overlay-spinner.directive';

describe('OverlaySpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlaySpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
